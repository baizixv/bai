# 项目代理规则

## 功能完成后的 Git 流程

- 每完成一个独立、可交付的功能后，自动执行必要检查，无需等待用户再次提醒。
- 检查通过后，自动创建一个语义清晰、仅包含该功能相关修改的 Git commit，并立即推送到当前分支对应的远端。
- 提交前必须查看 `git status` 和 diff，不得把用户已有的无关修改、临时文件、构建产物或敏感信息混入提交。
- 检查或推送失败时不得隐瞒或宣称成功；应保留本地修改或本地 commit，并向用户说明失败步骤和原因。
- 除非用户明确要求，否则不要使用 `--force`、不要改写历史，也不要跳过检查。
- 最终回复中简要说明检查结果、commit hash 和推送结果。

其他工程规范见 `docs/ARCHITECTURE.md`。
